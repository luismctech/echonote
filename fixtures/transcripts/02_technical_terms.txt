The new architecture uses a clean separation between the domain layer and the infrastructure adapters. We rely on hexagonal architecture with ports defined in the domain crate and concrete implementations injected at startup. The database is SQLite with write-ahead logging enabled.
